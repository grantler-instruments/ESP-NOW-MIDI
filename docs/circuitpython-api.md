# CircuitPython API

The CircuitPython implementation uses the same ESP-NOW packet format as the
C++ core. For Arduino and ESP-IDF C++, see the [API reference overview](api-reference.md).

::: esp_now_midi
